@model EDR.Models.ViewModels.Test2

@{
    ViewBag.Title = "How to Update multiple rows at once Using MVC 4 Razor and Entity Framework";
}
<script type="text/javascript">
    function ConfirmUpdate() {
        return confirm("Do you want to Update the records.");
    }
</script>
@using (@Html.BeginForm("Test2", "Home", FormMethod.Post))
{
    @Html.EditorFor(model => model.Members)
    @*<table>
    <tr>
        <th>
        </th>
        <th>
            Name
        </th>
        <th>
            Admin
        </th>
    </tr>
    @for (int i = 0; i < Model.Members.Count; i++)
    {
        <tr>
            <td>
                @Html.HiddenFor(model => model.Members[i].Id)
        </td>
        <td>
            @Html.EditorFor(model => model.Members[i].User.FirstName)
        </td>
        <td>
            @Html.EditorFor(model => model.Members[i].Admin)
        </td>
    </tr>
    }
    </table>*@
    <p>
        <input type="submit" value="Update" onclick="javascript:return ConfirmUpdate();" />
    </p>
    <p style="color: green; font-size: 12px;">
        @ViewBag.Message
    </p>
}