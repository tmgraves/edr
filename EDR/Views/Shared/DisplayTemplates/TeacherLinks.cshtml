@model IEnumerable<EDR.Models.Teacher>

@foreach (var item in Model.OrderBy(s => s.ApplicationUser.FullName))
{
    @Html.ActionLink(item.ApplicationUser.FullName, "Home", "Teacher", new { username = item.ApplicationUser.UserName }, null)<text> </text>
}
