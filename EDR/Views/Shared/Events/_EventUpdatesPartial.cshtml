@model IEnumerable<EDR.Models.EventMedia>

@if (Model.Count() > 0)
{
    foreach (var media in Model)
    {
        @Html.Partial("~/Views/Shared/_MediaPartial.cshtml", media)
    }
}
else
{
    @: No Updates
}

