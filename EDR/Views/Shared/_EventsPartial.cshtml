@model IEnumerable<EDR.Models.Event>

@if (Model.Count() != 0)
{
    foreach (var e in Model.OrderBy(c => c.EventInstances.Where(i => i.DateTime >= DateTime.Today).Min(i => i.DateTime)))
    {
        @Html.DisplayFor(m => e, "Event")
    }
}
else
{
    <h4>No Events</h4>
}

